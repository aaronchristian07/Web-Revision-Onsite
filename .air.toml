root = "."
tmp_dir = "tmp"

[builds]
    cmd = "go build -o ./tmp/main ."
    entrypoint = ["./tmp/main"] 
    include_ext = ["go"]
    exclude_dir = ["tmp"]
    poll = true
    poll_interval = 500
    delay = 100

[log]
time = true