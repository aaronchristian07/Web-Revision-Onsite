# Base URL the frontend uses for API calls.
# Relative path -> requests go through the nginx gateway (same origin, no CORS).
# Use an absolute URL (e.g. http://localhost:8001) only when running the
# frontend outside docker against a directly-exposed service.
VITE_API_URL=/api
