root = "."
tmp_dir = "tmp"

[build]
    cmd = "go build -o ./tmp/main ./cmd"
    entrypoint = ["./tmp/main"]
    include_ext = ["go"]
    exclude_dir = ["tmp"]
    poll = true
    poll_interval = 500
    delay = 100

[log]
time = true
